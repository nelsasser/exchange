apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
stringData:
  db_config.json: |
    "lol": "nerd"
  client-cert.pem: |
    get rekt
  client-key.pem: |
    ur ass is grass
  server-ca.pem: |
    and im the lawnmower